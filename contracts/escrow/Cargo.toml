[package]
name = "trustrent-escrow"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { version = "26.0.0" }

[dev-dependencies]
soroban-sdk = { version = "26.0.0", features = ["testutils"] }
